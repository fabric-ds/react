import { Toast, ToastContainer, useToast } from '../src';
import { Button } from '../../button/src';
import { Toggle } from '../../toggle/src';
import packageData from '../package.json';

# Toast

<PackageInfo
    packageData={packageData}
    componentNames={['ToastContainer', 'useToast', 'Toast']}
/>

<Toast type="success" text="hi" onClose={() => alert('hi')} />
<Toast type="error" text="hi" />
<Toast type="warning" text="hi" canClose={false} />
<Toast type="info" text="hi" />
<Toast type="loading" text="hi" />

### Automatically create toast

```jsx example
function Example() {
    const { toast } = useToast();

    const [type, setType] = React.useState();

    const createToast = () => {
        toast("Hi! I'm an example toast!", {
            type,
            duration: 4000,
        });
    };

    return (
        <>
            <Toggle
                type="radio"
                onChange={(v) => setType(v.value)}
                defaultSelected={[{ label: 'Success', value: 'success' }]}
                options={[
                    { label: 'Success', value: 'success' },
                    { label: 'Error', value: 'error' },
                    { label: 'Warning', value: 'warning' },
                    { label: 'Info', value: 'info' },
                    { label: 'Loading', value: 'loading' },
                ]}
            />
            <Button className="mt-10" onClick={createToast}>
                Create toast
            </Button>
        </>
    );
}

/* Root */
function App() {
    return (
        <ToastContainer>
            <Example />
        </ToastContainer>
    );
}
```

# Toast props

```props packages/toast/src/toast.tsx

```
