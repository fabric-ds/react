import * as React from 'react';
import { ToastContainer } from '../src';

const metadata = { title: 'Layout/Toast' };
export default metadata;

export const Example = () => {
    return <ToastContainer />;
};
